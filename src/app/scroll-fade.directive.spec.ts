import { ScrollFadeDirective } from './scroll-fade.directive';

describe('ScrollFadeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFadeDirective();
    expect(directive).toBeTruthy();
  });
});
